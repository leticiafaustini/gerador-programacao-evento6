# Copie este arquivo para ".env" e preencha com sua chave real.
# Nunca coloque a chave real neste arquivo (.env.example) nem suba ela pro Git.

# Chave de API da Anthropic (console.anthropic.com > API Keys)
ANTHROPIC_API_KEY=

# Porta em que o servidor vai rodar (opcional, padrão 3000)
PORT=3000
